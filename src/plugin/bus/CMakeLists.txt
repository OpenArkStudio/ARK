if(WIN32)
    if (_type STREQUAL debug)
        BUILD_PLUGIN_MACRO(AFBusPlugin AFProto brynetd.lib libprotobufd.lib)
    else()
        BUILD_PLUGIN_MACRO(AFBusPlugin AFProto brynet.lib libprotobuf.lib)
    endif()
else(UNIX)
    BUILD_PLUGIN_MACRO(AFBusPlugin AFProto brynet protobuf)
endif()

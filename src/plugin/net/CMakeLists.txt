if(WIN32)
    if (_type STREQUAL debug)
        BUILD_PLUGIN_MACRO_EX(AFNetPlugin AFProto brynetd.lib libprotobufd.lib)
    else()
        BUILD_PLUGIN_MACRO_EX(AFNetPlugin AFProto brynet.lib libprotobuf.lib)
    endif()
else(UNIX)
    BUILD_PLUGIN_MACRO_EX(AFNetPlugin AFProto brynet protobuf)
endif()
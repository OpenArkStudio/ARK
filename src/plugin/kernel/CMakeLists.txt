if(WIN32)
    if (_type STREQUAL debug)
        BUILD_PLUGIN_MACRO(AFKernelPlugin AFProto libprotobufd.lib)
    else()
        BUILD_PLUGIN_MACRO(AFKernelPlugin AFProto libprotobuf.lib)
    endif()
else(UNIX)
    #message("kernel plugin --------")
    BUILD_PLUGIN_MACRO(AFKernelPlugin AFProto protobuf)
endif()

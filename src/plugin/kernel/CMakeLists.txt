if(WIN32)
    if (_type STREQUAL debug)
        BUILD_PLUGIN_MACRO_EX(AFKernelPlugin AFProto libprotobufd.lib)
    else()
        BUILD_PLUGIN_MACRO_EX(AFKernelPlugin AFProto libprotobuf.lib)
    endif()
else(UNIX)
    BUILD_PLUGIN_MACRO_EX(AFKernelPlugin AFProto protobuf)
endif()